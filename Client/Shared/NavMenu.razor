<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="/">Odder Otter</a>
    <!-- The collapse button will only show on small screens -->
    <button class="navbar-toggler-icon d-md-none" @onclick="ToggleNavMenu" />
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="/" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </li>

        <li class="nav-item px-3">
            <NavLink class="nav-link" href="/todo">
                <span class="oi oi-clipboard" aria-hidden="true"></span> To Do List
            </NavLink>
        </li>
    </ul>
</div>


@code {
    private bool collapseNavMenu = false;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = collapseNavMenu;
    }
}